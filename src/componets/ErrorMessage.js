

const ErrorMessage = ({message}) => {
    return (
        <div>
            <p style={{ color: "red", fontSize: "18px", marginTop: "-8px" }}>{message}</p>
        </div>
    )
}

export default ErrorMessage;